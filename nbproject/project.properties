application.title=ab5k-tetris
application.vendor=francisdb
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/ab5k-tetris.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.desklet-specification-1.0.jar=../desklets/spec/target/desklet-specification-1.0.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.desklet-specification-1.0.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/home/francisdb/netbeans_workspace/ab5k-tetris/dist/
jnlp.enabled=false
jnlp.offline-allowed=false
main.class=eu.somatik.desklet.tetris.TetrisDesklet
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=jdk1.6.0_01
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Dsun.java2d.opengl=true
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.java.dir=src/main/java
src.resources.dir=src/main/resources
test.java.dir=src/test/java
test.src.dir=test
